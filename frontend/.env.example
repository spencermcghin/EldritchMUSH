# Copy to .env.local for local development
# These are embedded at build time by Vite (must start with VITE_)

# WebSocket host for the Evennia game server
VITE_GAME_HOST=localhost

# WebSocket port
VITE_GAME_PORT=4002
